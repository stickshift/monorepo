{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Lint executor",
  "description": "",
  "type": "object",
  "properties": {
    "fix": {
      "type": "boolean",
      "description": "Whether to automatically fix lint issues where possible",
      "default": false
    },
    "preview": {
      "type": "boolean",
      "description": "Whether to enable ruff's preview mode",
      "default": true
    }
  },
  "required": []
}
