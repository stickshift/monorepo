{
    "name": "monorepo",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "install:ts": "bun install",
        "install:py": "uv sync",
        "install:all": "bun run install:ts && bun run install:py",
        "dev:apps": "concurrently \"bun run --filter web-app dev\" \"bun run --filter service dev\"",
        "dev": "bun run dev:apps",
        "clean:apps": "bun run --filter web-app clean && bun run --filter service clean",
        "clean:root": "rm -rf node_modules .venv",
        "clean": "bun run clean:apps && bun run clean:root"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/bun": "^1.1.16",
        "@types/react": "^19.0.4",
        "concurrently": "^9.1.2",
        "typescript": "^5.7.3"
    }
}