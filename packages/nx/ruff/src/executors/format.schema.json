{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Format executor",
  "description": "",
  "type": "object",
  "properties": {
    "fix": {
      "type": "boolean",
      "description": "Whether to automatically fix issues where possible"
    },
    "preview": {
      "type": "boolean",
      "description": "Whether to enable ruff's preview mode"
    }
  },
  "required": ["fix", "preview"]
}
