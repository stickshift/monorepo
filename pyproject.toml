#-------------------------------------------------------------------------------
# Project: monorepo
#-------------------------------------------------------------------------------

[project]

name = "monorepo"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "service",
]

#-------------------------------------------------------------------------------
# Workspaces
#-------------------------------------------------------------------------------

[tool.uv.workspace]

members = ["packages/py-tools", "apps/service"]

[tool.uv.sources]

service = { workspace = true }

[dependency-groups]
dev = [
    "mypy>=1.14.1",
    "ruff>=0.9.0",
]
